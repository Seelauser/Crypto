import type { Metadata } from 'next';
import HomePage from '../page';

export const metadata: Metadata = {
  title: 'Try OrderFlow Beast — Free order-flow analytics',
  description:
    'See where smart money is actually moving. True L2 + AI-explained signals across 6 asset classes. Start free, no card.',
};

export default HomePage;
